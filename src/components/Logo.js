import React from 'react'
import logo from "../FoodImages/front-page-images/logo.png"

const Logo = () => {
    return (
        <div>
        <img className="logo-image" src={logo} alt="Not found"></img>
        </div>
    )
}

export default Logo
